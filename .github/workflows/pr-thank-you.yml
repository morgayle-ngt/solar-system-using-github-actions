on:
    pull_request:
      types:
      - opened
  
jobs:
  pr-action:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - name: Post PR Comment
      uses: morgayle-ngt/docker-action-pr-giphy-comment@main
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        giphy-api-key: ${{ secrets.GIPHY_API_KEY }}
    - name: Post Javascript Action PR Comment
      uses: morgayle-ngt/js-action-pr-giphy-comment@5c0d539a9a4ee62985a7632842212afe220fcf9c
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        giphy-api-key: ${{ secrets.GIPHY_API_KEY }}
# Version tag not working using master branch
